

export class ExportGenerator {
    public static generate(element: HTMLElement): void {
        // generate html file

        // generate css file
    }

    private static generateHTML(element: HTMLElement) {
        
    }

    private static generateCSS(element: HTMLElement) {

    }
}