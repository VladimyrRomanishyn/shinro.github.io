import { CheckEditorContentPipe } from './check-editor-content.pipe';

describe('CheckEditorContentPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckEditorContentPipe();
    expect(pipe).toBeTruthy();
  });
});
